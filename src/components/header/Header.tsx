import { Layout, Menu, Breadcrumb } from 'antd';

const { Header, Content, Footer } = Layout;

const AppHeader = () => {
  return (
    <Header>
      <p className="logo">Survey App</p>
    </Header>
  )
}

export default AppHeader;